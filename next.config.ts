import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  trailingSlash: false, // Certifique-se de que isso está de acordo com o esperado
};

export default nextConfig;
