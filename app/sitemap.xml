// app/sitemap.xml/route.js
import { arcadeGames } from '@/data/ArcadeGames';
import { snesGames } from '@/data/SnesGames';
import { atariGames } from '@/data/AtariGames';
import { gbaGames } from '@/data/GbaGames';
import { megadriveGames } from '@/data/MegadriveGames';

export const runtime = 'edge';

export async function GET() {
  const staticRoutes = [
    '',
    'gamelistArcade',
    'gamelistAtari',
    'gamelistGba',
    'gamelistMegadrive',
    'adult-games',
    'como-jogar',
  ];

  const baseUrl = 'https://gegames.vercel.app';

  const staticUrls = staticRoutes.map((route) => `
    <url>
      <loc>${baseUrl}/${route}</loc>
      <lastmod>${new Date().toISOString()}</lastmod>
      <changefreq>monthly</changefreq>
      <priority>0.8</priority>
    </url>
  `).join('');

  // Consolida todos os jogos
  const allGames = [
    ...arcadeGames,
    ...snesGames,
    ...atariGames,
    ...gbaGames,
    ...megadriveGames
  ];

  // Filtra apenas jogos com ID ou slug válido
  const gameUrls = allGames
    .filter(game => game?.id && game?.id !== 'undefined')
    .map(game => `
      <url>
        <loc>${baseUrl}/games/${encodeURIComponent(game.id)}</loc>
        <lastmod>${new Date().toISOString()}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.7</priority>
      </url>
    `).join('');

  const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
  <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    ${staticUrls}
    ${gameUrls}
  </urlset>`;

  return new Response(sitemap, {
    headers: {
      'Content-Type': 'application/xml',
    },
  });
}

